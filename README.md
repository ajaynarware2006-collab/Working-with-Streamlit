# 🚀 Streamlit Learning Journey

This repository documents my journey of learning **Streamlit** from scratch with a focus on building modern AI/ML web applications.

Instead of following random tutorials, I am learning by building practical challenges after every module and pushing my progress to GitHub.

---

# 🎯 Goal

Learn Streamlit deeply enough to build professional AI applications such as:

- AI Resume Analyzer
- House Price Predictor
- AI Chatbot
- Analytics Dashboard
- Expense Tracker
- ML Prediction Systems

This repository is part of my preparation for AI/ML internships and becoming an AI Engineer.

---

# 📚 Modules Completed

## ✅ Module 1 — Content & Typography

Learned:

- `st.title()`
- `st.header()`
- `st.subheader()`
- `st.write()`
- `st.markdown()`
- `st.caption()`
- `st.code()`
- `st.divider()`

Challenge:
- AI Portfolio Landing Page

---

## ✅ Module 2 — User Input Widgets

Learned:

- `st.text_input()`
- `st.text_area()`
- `st.number_input()`
- `st.date_input()`
- `st.time_input()`
- `st.selectbox()`
- `st.multiselect()`
- `st.radio()`
- `st.checkbox()`
- `st.toggle()`
- `st.slider()`
- `st.button()`

Challenge:
- Internship Registration Portal

---

## ✅ Module 3 — Layout & Organization

Learned:

- `st.sidebar()`
- `st.columns()`
- `st.container()`
- `st.tabs()`
- `st.expander()`
- `st.empty()`

Challenge:
- AI Dashboard Layout

---

## ✅ Module 4 — Data Display & Status Components

Learned:

- `st.metric()`
- `st.dataframe()`
- `st.table()`
- `st.json()`
- `st.success()`
- `st.warning()`
- `st.error()`
- `st.info()`
- `st.progress()`
- `st.spinner()`
- `st.status()`

Challenge:
- AI Analytics Dashboard

---

## ✅ Module 5 — Charts & Data Visualization

Learned:

- `st.line_chart()`
- `st.bar_chart()`
- `st.area_chart()`
- `st.pyplot()`
- `st.plotly_chart()`

Challenge:
- Business Intelligence Dashboard

---

## ✅ Module 6 — Media & File Handling

Learned:

- `st.image()`
- `st.audio()`
- `st.video()`
- `st.file_uploader()`
- `st.download_button()`

Challenge:
- AI Resume Analyzer UI

---

## ✅ Module 7 — Forms & Session State

Learned:

- `st.form()`
- `st.form_submit_button()`
- `st.session_state`
- `st.rerun()`

Challenge:
- AI Chat Interface

---

## ✅ Module 8 — Multipage Apps & Navigation

Learned:

- `st.set_page_config()`
- `st.Page()`
- `st.navigation()`

Challenge:
- AI Company Portal

---

## ✅ Module 9 — Rich Content & Utilities

Learned:

- `st.link_button()`
- Markdown hyperlinks
- `st.page_link()`
- Local & Online Images
- Markdown tables
- `st.logo()`
- `st.badge()` *(version dependent)*

Challenge:
- Professional AI Portfolio

---

## ✅ Module 10 — Styling & Custom CSS

Learned:

- Advanced `st.set_page_config()`
- Themes (`config.toml`)
- HTML in Streamlit
- Custom CSS
- Button Styling
- Cards
- Custom Fonts
- Background Styling
- Hide Default Streamlit UI
- Responsive Design Basics

Challenge:
- Professional AI SaaS Dashboard

---

# 🛠️ Tech Stack

- Python
- Streamlit
- Pandas
- Matplotlib
- Plotly

---

# 📌 Learning Approach

Instead of copying tutorials, I follow this workflow:

1. Learn a group of related Streamlit components.
2. Experiment with runnable examples.
3. Understand common mistakes.
4. Build a real-world challenge.
5. Commit progress to GitHub.

---

# 📅 Current Progress

- ✅ 10 / 13 Learning Modules Completed
- 🚧 PostgreSQL Integration (Next)
- 🚧 AI Application Development
- 🚧 Deployment

---

# 🎯 Final Goal

By the end of this journey, I will build production-ready Streamlit applications integrated with:

- PostgreSQL
- Machine Learning Models
- FastAPI
- Custom UI
- Deployment

---

## ⭐ Repository Status

🚧 Work in Progress — Updated continuously as I learn Streamlit.